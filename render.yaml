services:
  - type: web
    name: cityreport
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: cd backend && gunicorn app:app --bind 0.0.0.0:
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
